A filtering widget for weblocks.

Currently only visual interface available.
Usage is 

(make-instance 
 'weblocks-filtering-widget:filtering-widget 
 :form-fields (list 
     (list 
      :id :name
      :caption "Name"
      :accessor #'person-first-name)
     (list 
      :id :last-name 
      :caption "Last name"
      :accessor #'person-last-name)))

